### Workflow for NC-upper ###

A detailed description of each step is in https://rdatools.github.io/tradeoffs/workflow.

( ) Step 1 - Extract the data, shapes, and graph for the state

( ) Step 2 - Set up the state for trade-off analysis

( ) Step 3 - Create an approximate 'root map'

( ) Step 4 - Gather additional data points manually

( ) Step 5 - Generate & score an unbiased ensemble
    From 'rdaensemble' run:

    scripts/STEP_5.sh --state NC --plan-type upper

( ) Step 6 - Generate & score an optimized ensemble
    From 'rdaensemble' run:

    scripts/STEP_6.sh --state NC --plan-type upper

( ) Step 7 - Find the trade-off frontiers & generate the analysis artifacts
    From 'tradeoffs' run:

    scripts/STEP_7.sh --state NC --plan-type upper

( ) Step 8 - Deploy the artifacts

( ) Step 9 - Activate the state in the site

### END ###
